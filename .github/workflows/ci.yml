name: CI / build

on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']

jobs:
  build:
    name: build
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - run: echo "push build ok"

  pr-status:
    name: pr-status
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - run: echo "pr status ok"
